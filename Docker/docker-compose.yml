version: '3.8'

services:
    mongodb:
        image: mongo
        container_name: mongodb
        restart: always
        ports:
            - "27017:27017"
        networks:
            - mongodb_network

networks:
    mongodb_network:
        driver: bridge
